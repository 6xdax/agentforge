# AgentForge Configuration Template
# Copy to .env and fill in values

# =============================================================================
# .env — Sensitive (API keys, secrets) — NEVER commit to version control
# =============================================================================

# LLM API Key (required)
MINIMAX_API_KEY=your_api_key_here

# =============================================================================
# config/llm.yml — Non-sensitive defaults (model, base_url, max_tokens)
# config/settings.py — Pydantic Settings (env vars override YAML defaults)
# =============================================================================
